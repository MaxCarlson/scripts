

# Ideas

## git stuff 1 
Cycle through a list of stages / unstaged values?
    - easy one click restore/full related subfolder path restore 
    - easy one click diff
    whatever other info i mayne be missing

## Function/Fast to use alias or hotkey that

- that runs the previous command `fn() | xargs ...etc` 
  - but with `--gn-0=fn()` 
  - much simpler explanation,
    - allow user to run prev command, with new command replacing precious imdex-0 command
