# Stage 1: install and verify

Implement the shared missing-only inventory, thin platform launchers, and regression tests. Verify the dry run and rerun/idempotence behavior without executing real package installation during tests. Stop for user validation before commit.
