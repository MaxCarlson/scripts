<!-- development-ledger:generated-template:v1 -->
# Scoped Documentation: `{{SCOPE_PATH}}`

This `docs/` directory owns planning, handoffs, and development-ledger evidence for `{{SCOPE_PATH}}` and descendants unless a more-specific nested scope exists.

- `HANDOFF.md` records the current stopping point and next actor for this scope.
- `plans/` contains scoped implementation plans.
- Each active plan may contain a generated `ledger/` directory.
- Shared repository workflow: `{{ROOT_WORKFLOW_PATH}}`.

Do not place sibling-scope or repository-wide progress here unless an explicit coordinating plan requires it.
