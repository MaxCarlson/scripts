<!-- version: 0.1.0 -->
# llm-patcher

Apply LLM-generated repository edits using the strict LEP/v1 patch format with validation-oriented safeguards.

## Usage

```bash
llm-patcher --help
```

Install from the repository root with `python -m pip install -e modules/llm-patcher`.
