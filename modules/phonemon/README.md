<!-- version: 0.1.0 -->
# phonemon

Phone-friendly, Termux-ready terminal system monitor with a `btop`-style interface.

## Usage

```bash
phonemon --help
```

Install from the repository root with `python -m pip install -e modules/phonemon`.
