<!-- version: 0.3.0 -->
# agt

Lightweight terminal client for WebAI-to-API services, with Gemini profiles, streaming output, token reporting, and a terminal UI.

## Usage

```bash
agt --help
```

Install from the repository root with `python -m pip install -e modules/agt`.
