<!-- version: 0.17.0 -->
# vdedup

Video and file deduplication toolkit with a staged comparison pipeline, resumable caching, GPU-assisted analysis options, and a responsive terminal UI.

## Usage

```bash
video-dedupe --help
```

Install from the repository root with `python -m pip install -e modules/vdedup`.
