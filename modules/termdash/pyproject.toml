[project]
name = "termdash"
version = "0.1.0"
description = "A robust, thread-safe library for persistent terminal dashboards"
readme = "README.md"
authors = [
    { name = "Your Name", email = "your@email.com" }
]
license = "MIT"
requires-python = ">=3.8"

# This section tells the installer how to find your code.
# By leaving it empty, it defaults to looking in the current directory ("."),
# which is correct for your new flat layout.
[tool.setuptools.packages.find]

[project.urls]
Homepage = "https://github.com/yourname/termdash"
Repository = "https://github.com/yourname/termdash"
