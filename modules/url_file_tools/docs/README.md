# URL File Tools Documentation

This module owns URL-list normalization, merge planning, registered-domain
grouping, duplicate reporting, and the dry-run/apply CLI contract.

The active implementation plan is indexed by `docs/plans/HANDOFF.md`.
