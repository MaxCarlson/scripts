# Plans Handoff

Active plan: `20260926-0041_url-file-tools/`

The plan introduces a standalone `url-files` package rather than coupling
URL-specific parsing and Public Suffix List behavior to generic `file_utils`.
