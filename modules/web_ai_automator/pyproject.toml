[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "web-ai-automator"
version = "0.1.0"
description = "Automate web AI platforms with Selenium and Flask API."
authors = [
    { name = "Your Name", email = "your@email.com" }
]
readme = "README.md"
dependencies = [
    "flask",
    "selenium"
]
requires-python = ">=3.8"

[project.scripts]
web-ai-automator = "web_ai_automator.cli:main"
