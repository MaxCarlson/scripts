[build-system]
requires = ["setuptools>=67", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sshmanager"
version = "0.1.0"
description = "Simple SSH connection manager with encrypted host definitions"
authors = [{ name = "AutoGenerated", email = "noreply@example.com" }]
requires-python = ">=3.8"
dependencies = [
    "cryptography>=3.4",
]

[project.scripts]
sshmanager = "sshmanager.cli:main"