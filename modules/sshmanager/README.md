<!-- version: 0.1.0 -->
# sshmanager

Simple SSH connection manager with encrypted host definitions.

## Usage

```bash
sshmanager --help
```

Install from the repository root with `python -m pip install -e modules/sshmanager`.
