<!-- version: 0.1.0 -->
# backup_module

Restic-backed backup, search, restore, and scheduling command-line tool.

## Usage

```bash
backup_module --help
```

Install from the repository root with `python -m pip install -e modules/backup_module`.
