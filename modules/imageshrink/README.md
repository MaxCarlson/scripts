<!-- version: 0.5.0 -->
# imageshrink

Multi-process image shrinker intended for manga/image collections, with a live TermDash-style progress display.

## Usage

```bash
imgshrink --help
```

Install from the repository root with `python -m pip install -e modules/imageshrink`.
