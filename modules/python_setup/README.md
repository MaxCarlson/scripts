<!-- version: 0.2.0 -->
# python_setup

Python environment bootstrap and best-practice setup tools for Ubuntu/WSL2, Termux, and Windows PowerShell 7.

## Usage

```bash
python-setup --help
setup-venv-activation --help
```

Install from the repository root with `python -m pip install -e modules/python_setup`.
