<!-- version: 0.2.0 -->
# dlmanager

Multi-method transfer manager for `rsync`, `rclone`, and `scp` jobs with live progress reporting.

## Usage

```bash
dlmanager --help
```

Install from the repository root with `python -m pip install -e modules/dlmanager`.
