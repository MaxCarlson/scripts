<!-- version: 3.0.0 -->
# filter-prune

Dry-run-first file operations driven by `fd`/`rg` filters. It supports deletion, moving, quarantine, command execution, and concatenation workflows while requiring deliberate mutation.

## Usage

```bash
filter-prune --help
```

Install from the repository root with `python -m pip install -e modules/filter_prune`.
