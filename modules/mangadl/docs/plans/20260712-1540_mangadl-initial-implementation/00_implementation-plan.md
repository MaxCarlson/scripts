# mangadl Initial Implementation

Implement the approved repository-level plan as a new additive Python package: package and models; input and routing; SQLite leases/recovery; supervised workers, logs, metrics and retries; CLI/dashboard; registry, docs, and tests.

No existing downloader is migrated or deleted. No database migration machinery is implemented; incompatible schema versions fail clearly.
