<!-- version: 0.3.0 -->
# sysmon

Cross-platform system monitor terminal UI for CPU, memory, disk, network, and GPU activity, with capture support.

## Usage

```bash
sysmon --help
sysmon-capture --help
```

Install from the repository root with `python -m pip install -e modules/sysmon`.
