<!-- version: 0.1.0 -->
# lmstui

Termux-friendly command-line and terminal UI helpers for LM Studio REST and OpenAI-compatible servers.

## Usage

```bash
lmstui --help
```

Install from the repository root with `python -m pip install -e modules/lmstui`.
